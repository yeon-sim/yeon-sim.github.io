baseURL = 'https://yeon-sim.github.io/'
ignoreFiles = ['[/\\\\]temp[/\\\\]']
languageCode = 'ko'
title = '연심(連心)'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[taxonomies]

[server]
  [[server.headers]]
    for = "/**"
    [server.headers.values]
      Cache-Control = "no-store"
